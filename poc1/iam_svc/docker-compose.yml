version: '3.6'

services:
  postgres:
    image: postgres:11-alpine
    restart: always
    container_name: opa_poc1_iam_svc_pgdb
    environment:
      POSTGRES_USER: iam
      POSTGRES_PASSWORD: iam
      POSTGRES_DB: iam
    ports:
      - 5433:5432
